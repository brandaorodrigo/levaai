import { Layout, Typography } from 'antd';

const Erro = () => {
    return (
        <Layout>
            <Typography.Title level={1}>Error</Typography.Title>
        </Layout>
    );
};

export default Erro;
